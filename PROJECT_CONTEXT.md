# Project context

Why GroundTruth exists, what it is for, and the constraints that are not
negotiable. Read this before `ARCHITECTURE.md` — the architecture only makes
sense once the problem does.

---

## The problem

Voluntary carbon markets sell **additionality**: the climate benefit that
exists *because* a project happened and would not have existed otherwise.

That is a counterfactual quantity. It describes a world that never occurred, so
it cannot be measured — only estimated. Today it is estimated largely by the
project developer, using a baseline the developer proposes, validated by an
auditor the developer selects and pays. The party with the strongest financial
interest in a high number produces the number.

The gap this creates is documented. Following a two-year investigation
concluded in 2025, the registry Verra found that a reported **57% of the ~27
million credits issued by the Kariba REDD+ project** (Zimbabwe, 785,000 ha)
were in excess of the emission reductions actually achieved.

*That figure is Verra's, not ours.* It is recorded in `cases/kariba-redd.yaml`
as a validation target the pipeline never reads. It matters to us for one
reason: it is one of the few projects where an independent method can be
**benchmarked against a published third-party correction**.

## The insight

Before-and-after satellite comparison is not verification.

```
naive:        effect = indicator_after − indicator_before
GroundTruth:  effect = indicator_project − indicator_counterfactual
```

The naive form absorbs every regional trend — rainfall cycles, commodity
prices, road building, conflict — into the estimate. A project in a region where
deforestation slowed for unrelated reasons looks successful.

The counterfactual is built from regions comparable on rainfall, elevation,
slope, road access and settlement pressure that were not subject to the
intervention. If a weighted combination of them tracks the project for a decade
*before* the intervention, their later path is a defensible estimate of what the
project area would have done.

## What GroundTruth is

A **screening and decision-support system**: a way to decide which claims
deserve a closer, expensive, accredited look.

| User | What they get |
| --- | --- |
| Carbon-credit buyers, ESG teams | Fast pre-purchase screening before committing capital |
| Registries and auditors | A triage signal for which projects to review first |
| Climate-finance investors | Independent portfolio-level risk assessment |
| Legitimate restoration developers | Third-party corroboration of a genuine claim |
| Researchers and journalists | A reproducible, provenance-carrying method |

## What GroundTruth is not

This list is load-bearing. Several architectural decisions exist only to make
these impossible rather than merely discouraged.

- **Not a fraud detector.** Divergence between an independent estimate and a
  claim is a reason to look harder. Nothing in a satellite time series can
  establish intent, and no code path may produce text that says otherwise.
- **Not a carbon accounting system.** It does not issue, retire or price
  credits.
- **Not a replacement for accredited field auditing.** Ground plots measure
  carbon pools no satellite can see.
- **Not a basis for enforcement.** No project should lose status on this output
  alone.
- **Not a prediction system.** The question is counterfactual, not predictive.
  A classifier can say what land cover *is*; it cannot say what it *would have
  been*.

## Three rules the system will not break

**NDVI is not carbon.** A spectral index is a reflectance ratio; carbon is a
mass. NDVI saturates over closed canopy, responds to moisture, has no depth, and
converting it requires site-specific allometry with 20–30% error at plot level.
Enforced in code by `contracts.units.assert_not_index_to_carbon`.

**Observed change is not causal effect.** Every estimate is conditional on its
donor pool, its pre-treatment fit and its identifying assumptions, and those
conditions travel with the number.

**Model divergence is not fraud.** Possible explanations for a divergence
include an optimistic baseline, an inadequate donor pool making *our* estimate
wrong, leakage, an indicator that does not track what the project protects, a
sensor transition, or genuine over-issuance. Only the last is misconduct, and
satellite data cannot distinguish them.

## Current honest status

**No analysis of any real project has been run.** Everything the pipeline
currently produces comes from a deterministic simulated fixture, labelled as
such in its provenance, its run warnings, its verdict caveats, the API response
and a banner on stderr.

What *is* established: the machinery is correct. The estimator recovers a known
injected effect on a panel generated by the process it assumes, the placebo
suite distinguishes that from a null, and the grounding layer demonstrably
rejects a hallucinating narrator.

The most informative thing the system currently does is **refuse**. Kariba
returns `INCONCLUSIVE` because an NDVI effect and a tCO2e claim are not
commensurable. Closing that gap needs a biomass model with propagated error,
which is scoped and deferred rather than approximated.

See [`docs/product/capability-matrix.md`](docs/product/capability-matrix.md) for
the full implemented / planned / future breakdown.

## Competition context

| | |
| --- | --- |
| Event | PCCOE International Grand Challenge 2026, Indradhanu, Pune |
| Theme | AI for Climate Change |
| Track | 5 — Biodiversity, Ecosystem Conservation & Climate Awareness |
| Topic | Ecosystem Restoration |
| Title | Restoration & Carbon-Credit Causal Impact Verifier |
| Team | Viraj Jadhao (CE, PESMCOE), Bhumi Sirvi (IT, PESMCOE) |

The hackathon is the origin, not the ceiling. The repository is structured for a
system that keeps growing for years — see [`ROADMAP.md`](ROADMAP.md).

## Team

| Owner | Share | Domain |
| --- | --- | --- |
| **Viraj** | ~40% | Analytical engine and the entire user-facing visual experience — the two areas that decide scientific credibility and whether the demo lands |
| **Bhumi** | ~60% | Platform and application engineering — everything that makes GroundTruth a real, deployable system |

The split is enforced architecturally, not by convention. See
[`OWNERSHIP.md`](OWNERSHIP.md).

## Where we could be wrong

The honest summary: this method could produce a confident estimate that is
simply incorrect, because the donor pool was inadequate in a way the balance
statistics did not reveal, or because a sensor transition introduced a step the
pre-period fit absorbed.

The mitigations — placebo suite, specification curve, refusal past a failed gate
— reduce that risk. They do not eliminate it. That is why the output is a
screening signal recommending accredited review, and not a verdict.
